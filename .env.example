# SMTP Configuration for Postfix
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

# Application
PORT=3000
NODE_ENV=production
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=Email Service